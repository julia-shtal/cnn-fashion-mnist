# cnn_project
CNN experiments on noisy vs. cleaned image data, including tools to convert labeled PNG folders into NPZ datasets and a LeNet‑based PyTorch script that compares training on corrected and uncorrected datasets across multiple evaluation scenarios.
